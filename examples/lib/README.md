# Libraries

Reusable Glass modules, imported with `import "…"`.

- [`prelude.glass`](prelude.glass) — the standard prelude: generic helpers the builtins don't cover (`nth`, `take`/`drop`, `zip`, `sum`/`product`, Option/Result combinators).
- [`rational.glass`](rational.glass) — exact rational arithmetic.
