# Showcase

What Glass's type system can *express* — the interesting end of the language.
Refinement types, effect rows, linear types, and a gallery of "exotic type"
demos (quantum amplitudes, causal graphs, geometry, logic, …) that use the
type checker to enforce real invariants at compile time.

Pick any file and read it; they're written to be read.
