# v4.68: negative case — a linear resource used TWICE. prism's
# parse-time linearity check rejects it: "used 2 times (no cloning)".
fn bad(seed: Int) : Int =
  let lin token = seed in
  token + token
bad(5)
